# Local .terraform directories
.terraform/

# Lambda build artifacts (archive_file output)
.build/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Include env-specific tfvars (demo values, no secrets)
!**/env/*.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# AI coding agents - local state, settings, and caches
.claude/
# Ephemeral session plan; durable backlog lives in GitHub Issues
tasks/todo.md
.cursor/
.aider*
.continue/
.codeium/

# Bootstrap-launched instance IDs (terraform-query-import lab), local-only
created-instances.txt

# OS-generated files
.DS_Store
Thumbs.db

# Editor/IDE local config
.vscode/
.idea/
*.swp
*.swo

# Environment and secrets
.env
.env.*
!.env.example
