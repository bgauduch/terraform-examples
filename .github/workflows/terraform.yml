name: Terraform quality

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]

concurrency:
  group: terraform-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  discover:
    name: Discover Terraform root modules
    # Always run on push; on pull_request, run only for fork PRs
    # (same-repo PRs are already covered by the push event).
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name != github.repository
    runs-on: ubuntu-latest
    outputs:
      cells: ${{ steps.find.outputs.cells }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0 # the change scope below diffs against main
      - id: find
        # Any directory under examples/ containing providers.tf is a root module. Only the roots a
        # change can reach are validated: a changed file selects every root it sits under, and a
        # changed file that sits under NO root selects every root of its example - it may be a
        # shared module those roots source, and that would otherwise go untested.
        # A change outside examples/ (CI, toolchain, repo doc) selects every root, as does an
        # empty diff. name = path minus examples/ ; example = examples/<name> (per-example tooling).
        run: |
          roots=$(find examples -name providers.tf -printf '%h\n' | sort)
          changed=$(git diff --name-only origin/main...HEAD || true)
          if [ -z "$changed" ] || printf '%s\n' "$changed" | grep -qvE '^examples/'; then
            selected="$roots"
          else
            selected=$(printf '%s\n' "$changed" | while IFS= read -r f; do
              printf '%s\n' "$roots" | grep -F -x "$(dirname "$f")" \
                || printf '%s\n' "$roots" | grep -E "^$(printf '%s' "$f" | cut -d/ -f1,2)(/|$)"
            done | sort -u)
          fi
          [ -n "$selected" ] || selected="$roots"
          echo "Root modules to validate:"; printf '%s\n' "$selected" | sed 's/^/  /'
          cells=$(printf '%s\n' "$selected" \
            | jq -R -s '
                split("\n")
                | map(select(length > 0))
                | map({
                    dir: .,
                    name: (sub("^examples/"; "")),
                    example: (split("/")[0:2] | join("/"))
                  })')
          echo "cells=$(echo "$cells" | jq -c .)" >> "$GITHUB_OUTPUT"

  fmt:
    name: terraform fmt
    needs: discover
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: ./.github/actions/tf-setup
      - run: terraform fmt -check -recursive -diff

  quality:
    name: validate + lint + scan (${{ matrix.cell.name }})
    needs: discover
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        cell: ${{ fromJSON(needs.discover.outputs.cells) }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - uses: ./.github/actions/tf-setup
        with:
          working-directory: ${{ matrix.cell.dir }}

      - name: terraform init (no backend)
        working-directory: ${{ matrix.cell.dir }}
        run: terraform init -backend=false -input=false

      - name: terraform validate
        working-directory: ${{ matrix.cell.dir }}
        run: terraform validate -no-color

      - name: tflint init
        working-directory: ${{ matrix.cell.dir }}
        run: tflint --init --config "${{ github.workspace }}/${{ matrix.cell.example }}/.tflint.hcl"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: tflint
        working-directory: ${{ matrix.cell.dir }}
        run: tflint --recursive --format compact --config "${{ github.workspace }}/${{ matrix.cell.example }}/.tflint.hcl"

      # trivy is installed by tf-setup (mise.toml) - no separate setup step.
      - name: trivy config scan (report + gate)
        working-directory: ${{ matrix.cell.dir }}
        run: |
          # Scan once to JSON, then convert the same results twice:
          #   - report: MEDIUM,HIGH,CRITICAL, never fails the job (visibility).
          #   - gate:   HIGH,CRITICAL, fails the job (exit-code 1) if any are found.
          trivy config . --format json --output trivy.json --exit-code 0

          echo "::group::Trivy findings (MEDIUM,HIGH,CRITICAL - report only)"
          trivy convert --format table --severity MEDIUM,HIGH,CRITICAL --exit-code 0 trivy.json
          echo "::endgroup::"

          trivy convert --format table --severity HIGH,CRITICAL --exit-code 1 trivy.json
